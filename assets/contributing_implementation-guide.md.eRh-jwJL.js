import{_ as e,i as r,o as t,an as n}from"./chunks/framework.pLoDVelM.js";const h=JSON.parse('{"title":"函数设计与实现指南","description":"","frontmatter":{},"headers":[],"relativePath":"contributing/implementation-guide.md","filePath":"contributing/implementation-guide.md","lastUpdated":1760199021000}'),i={name:"contributing/implementation-guide.md"};function s(l,o,a,c,d,g){return t(),r("div",null,[...o[0]||(o[0]=[n('<h1 id="函数设计与实现指南" tabindex="-1">函数设计与实现指南 <a class="header-anchor" href="#函数设计与实现指南" aria-label="Permalink to “函数设计与实现指南”">​</a></h1><p>欢迎参与 <code>Dora Pocket</code> 的代码贡献！本指南旨在为所有函数的设计与实现提供一套统一的、高质量的标准。遵循这些规范，将有助于我们共同维护一个稳定、可预测且易于维护的代码库。</p><h2 id="核心设计哲学" tabindex="-1">核心设计哲学 <a class="header-anchor" href="#核心设计哲学" aria-label="Permalink to “核心设计哲学”">​</a></h2><p>在编写任何代码之前，请理解并遵循以下核心哲学：</p><ul><li><strong><code>@esdora/kit</code> 的纯粹性：</strong> <code>kit</code> 包是整个生态的基石，必须严格遵守<strong>零依赖</strong>原则。任何需要外部依赖的功能，都应考虑创建一个新的独立包。</li><li><strong>不重复造轮子（但有例外）：</strong> 优先考虑社区中经过战斗考验的优秀库。但如果所需功能非常小且稳定，可以考虑“供应商化 (Vendoring)”以保持 <code>kit</code> 的纯粹性。</li><li><strong>用户体验优先：</strong> API 的设计应力求直观、易用，并提供清晰的错误提示。</li></ul><h2 id="目录结构规范" tabindex="-1">目录结构规范 <a class="header-anchor" href="#目录结构规范" aria-label="Permalink to “目录结构规范”">​</a></h2><p>函数的存放位置取决于其<strong>稳定性</strong>和<strong>功能分类</strong>。</p><ul><li><strong>稳定函数：</strong> 存放于 <code>src/&lt;category&gt;/</code> 目录下。例如，一个稳定的函数工具应放在 <code>src/function/</code>。</li><li><strong>实验性函数：</strong> 必须存放于顶级的 <code>src/experimental/</code> 目录下。这能从物理上将它们与稳定代码隔离。</li></ul><h2 id="api-稳定性与生命周期管理" tabindex="-1">API 稳定性与生命周期管理 <a class="header-anchor" href="#api-稳定性与生命周期管理" aria-label="Permalink to “API 稳定性与生命周期管理”">​</a></h2><p>我们通过一套明确的机制来管理 API 的稳定性。</p><h4 id="_1-实验性-api-experimental" tabindex="-1">1. 实验性 API (<code>@experimental</code>) <a class="header-anchor" href="#_1-实验性-api-experimental" aria-label="Permalink to “1. 实验性 API (@experimental)”">​</a></h4><p>对于任何不确定或未来可能变更的 API，必须遵循以下所有规则：</p><ul><li><strong>存放位置：</strong> 必须位于 <code>src/experimental/</code> 目录下。</li><li><strong>命名约定：</strong> 函数名必须加上 <code>_unstable_</code> 前缀，例如 <code>_unstable_deepSort</code>。</li><li><strong>TSDoc 注释：</strong> 必须在 TSDoc 中添加 <code>@experimental</code> 标签，并明确说明其不稳定性。</li><li><strong>导出方式：</strong> 只能从专门的实验性入口（如 <code>@esdora/packages/kit/experimental</code>）导出。</li></ul><h4 id="_2-api-的-毕业" tabindex="-1">2. API 的“毕业” <a class="header-anchor" href="#_2-api-的-毕业" aria-label="Permalink to “2. API 的“毕业””">​</a></h4><p>当一个实验性 API 经过充分测试和社区反馈，准备成为稳定版时，需要执行以下步骤：</p><ol><li><strong>移动目录：</strong> 将其从 <code>src/experimental/</code> 移动到合适的稳定分类目录。</li><li><strong>重构代码：</strong> 移除函数名的 <code>_unstable_</code> 前缀。</li><li><strong>更新注释：</strong> 移除 <code>@experimental</code> 标签，并更新 TSDoc 为正式文档。</li><li><strong>更新导出：</strong> 将其从稳定入口（如 <code>@esdora/kit</code>）导出。</li><li><strong>记录变更：</strong> 在 <code>CHANGELOG</code> 中将此变动标记为新功能 (<code>feat</code>)。</li></ol><h2 id="tsdoc-注释规范" tabindex="-1">TSDoc 注释规范 <a class="header-anchor" href="#tsdoc-注释规范" aria-label="Permalink to “TSDoc 注释规范”">​</a></h2><p>高质量的 TSDoc 注释是代码不可或缺的一部分。</p><ul><li><strong>职责分离原则：</strong><ul><li><strong>简单函数：</strong> 在函数声明上方编写一个完整的、详尽的注释块。</li><li><strong>带重载的函数：</strong><ul><li>在<strong>每个重载签名</strong>上方，编写<strong>简洁</strong>的注释（摘要、<code>@param</code>, <code>@returns</code>），服务于 IDE 的智能提示。</li><li>在<strong>实现签名</strong>上方，编写<strong>最详尽</strong>的注释（<code>@remarks</code>, <code>@example</code> 等），作为技术底稿和文档源。</li></ul></li></ul></li><li><strong>语言：</strong> 所有 TSDoc 注释都推荐使用<strong>中文</strong>编写。</li><li><strong>示例为王 (<code>@example</code>)：</strong> 必须为公开的 API 提供清晰、可运行的示例。</li></ul><h2 id="依赖管理策略" tabindex="-1">依赖管理策略 <a class="header-anchor" href="#依赖管理策略" aria-label="Permalink to “依赖管理策略”">​</a></h2><ul><li><strong><code>@esdora/kit</code>:</strong> 严禁添加任何 <code>dependencies</code>。</li><li><strong>功能扩展：</strong> 当需要集成第三方库时（如颜色转换库 <code>color</code>），应创建一个新的、专注的独立包（如 <code>@esdora/color</code>），并在其中管理依赖。</li><li><strong>元包 <code>esdora</code>:</strong><ul><li><code>esdora</code> 包作为所有功能的统一入口。</li><li>它通过<strong>命名空间导出</strong>的方式 (<code>import * as kit from &#39;@esdora/kit&#39;</code>) 来聚合所有子包，以避免命名冲突并保持 API 结构清晰。</li></ul></li></ul><h2 id="新包的创建与发布" tabindex="-1">新包的创建与发布 <a class="header-anchor" href="#新包的创建与发布" aria-label="Permalink to “新包的创建与发布”">​</a></h2><p>当一个新功能需要引入第三方依赖时，或者它本身是一个相对独立的功能集合，就应该创建一个新的包。以下是将一个新包从创建到实现自动化发布的完整流程。</p><h3 id="_1-创建与初始化" tabindex="-1">1. 创建与初始化 <a class="header-anchor" href="#_1-创建与初始化" aria-label="Permalink to “1. 创建与初始化”">​</a></h3><ol><li><strong>创建目录</strong>: 在 <code>packages/</code> 目录下，以新包的名称创建一个新的文件夹。</li><li><strong>初始化 <code>package.json</code></strong>: 在新目录中，创建一个标准的 <code>package.json</code> 文件，确保包含 <code>name</code> (例如 <code>@esdora/new-package</code>), <code>version</code>, <code>description</code> 等关键字段。</li><li><strong>配置构建与测试</strong>: 参照现有包（如 <code>@esdora/color</code>）的结构，添加 <code>tsconfig.json</code>, <code>vitest.config.ts</code> 等构建和测试配置文件。</li></ol><h3 id="_2-首次发布-引导流程" tabindex="-1">2. 首次发布：引导流程 <a class="header-anchor" href="#_2-首次发布-引导流程" aria-label="Permalink to “2. 首次发布：引导流程”">​</a></h3><p>由于 npm 不允许为一个不存在的包预先配置 OIDC 可信发布，因此新包的<strong>首次发布</strong>需要一个手动的引导过程。</p><p><strong>核心思路：</strong> 使用一个临时的 <code>NPM_TOKEN</code> 进行第一次发布，然后在 npm 上为这个已存在的包配置 OIDC，最后立即销毁该临时 Token。</p><p><strong>操作步骤：</strong></p><ol><li><p><strong>生成临时 Token</strong>:</p><ul><li>访问 <a href="https://www.npmjs.com/" target="_blank" rel="noreferrer">npmjs.com</a>，登录您的账户。</li><li>进入 <strong>Access Tokens</strong> 页面，选择 <strong>Generate New Token</strong> -&gt; <strong>Granular Access Token</strong>。</li><li><strong>权限 (Permissions)</strong>: 选择 <code>Read and Write</code>。</li><li><strong>适用范围 (Scope)</strong>: 限制此 Token 仅对您所在的 <code>@esdora</code> 组织生效。</li><li><strong>安全 (Security)</strong>: 为了安全，建议开启双因素认证 (2FA)。</li><li>生成后，<strong>立即复制这个 Token</strong>，因为页面刷新后将无法再次看到。</li></ul></li><li><p><strong>配置 GitHub Secret</strong>:</p><ul><li>在 <code>esdora-js/esdora</code> 仓库的 <strong>Settings</strong> -&gt; <strong>Secrets and variables</strong> -&gt; <strong>Actions</strong> 页面。</li><li>创建一个名为 <code>NPM_TOKEN</code> 的新的仓库 Secret，并将上一步生成的 Token 粘贴进去。</li></ul></li><li><p><strong>触发发布</strong>:</p><ul><li>此时，<code>release.yml</code> 工作流在发布步骤中如果检测到 <code>NPM_TOKEN</code>，会优先使用它来发布。</li><li><strong>注意</strong>: 如果您的 npm 组织开启了发布保护，请确保在首次发布前，暂时将包的发布权限设置为 <code>Require two-factor authentication or an automation or granular access token</code>，以允许临时令牌生效。</li><li>按照正常的 <code>changeset</code> 流程，创建一个包含新包的版本变更，然后合并到 <code>main</code> 分支，这将触发发布流程，并将您的新包首次推送到 npm。</li></ul></li></ol><h3 id="_3-配置-oidc-可信发布" tabindex="-1">3. 配置 OIDC 可信发布 <a class="header-anchor" href="#_3-配置-oidc-可信发布" aria-label="Permalink to “3. 配置 OIDC 可信发布”">​</a></h3><p>首次发布成功后，您的包就已经存在于 npm 注册表上。现在，我们可以为其配置 OIDC。</p><ol><li><strong>登录 npm</strong>: 访问 <a href="https://www.npmjs.com/" target="_blank" rel="noreferrer">npmjs.com</a> 并登录。</li><li><strong>找到新包</strong>: 在您的包列表中，找到刚刚发布的新包，并进入其管理页面。</li><li><strong>进入设置</strong>: 点击 <strong>Settings</strong> 标签页。</li><li><strong>提升安全级别</strong>: 在 <strong>Publishing access</strong> 部分，选择最严格的 <strong><code>Require two-factor authentication and disallow tokens (recommended)</code></strong> 选项。 <ul><li><strong>重要提示</strong>: 根据 npm 官方说明，此选项与 OIDC 可信发布者完全兼容。启用后，将禁止使用传统的 <code>NPM_TOKEN</code> 进行发布，从而极大地提升了账户安全性。</li></ul></li><li><strong>配置可信发布者</strong>: <ul><li>在页面左侧找到 <strong>Trusted Publisher</strong> 菜单。</li><li><strong>发布者 (Publisher)</strong>: 选择 <code>GitHub Actions</code>。</li><li><strong>GitHub 组织 (Organization)</strong>: 填写 <code>esdora-js</code>。</li><li><strong>GitHub 仓库 (Repository)</strong>: 填写 <code>esdora</code>。</li><li><strong>工作流文件名 (Workflow filename)</strong>: 填写 <code>release.yml</code>。</li><li><strong>环境名 (Environment name)</strong>: (可选) 填写用于发布的 GitHub Actions 环境名。这是一个高级安全选项，用于在发布前引入人工审批流程。对于大多数项目，可以留空。</li><li>保存配置。</li></ul></li></ol><h3 id="_4-清理工作-移除临时-token" tabindex="-1">4. 清理工作：移除临时 Token <a class="header-anchor" href="#_4-清理工作-移除临时-token" aria-label="Permalink to “4. 清理工作：移除临时 Token”">​</a></h3><p>在 OIDC 配置完成并确认可以正常工作后（例如，在下一次发布中），<strong>必须立即执行以下清理操作</strong>：</p><ul><li>回到 GitHub 仓库的 <strong>Secrets</strong> 设置页面。</li><li><strong>彻底删除</strong>之前创建的 <code>NPM_TOKEN</code>。</li></ul><p>至此，您的新包已经完全接入了安全、无密码的 OIDC 发布流程。后续的所有发布都将由 GitHub Actions 自动完成，无需任何手动干预。</p>',37)])])}const m=e(i,[["render",s]]);export{h as __pageData,m as default};
